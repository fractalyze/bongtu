{
  "extends": "./tsconfig.base.json",
  "// note": "Type-checks the runnable scripts (deploy/, circuits/, chains/evm/test/fixtures/) plus the sdk sources they import directly via NodeNext .js specifiers. The sdk has its own tsconfig for `npm test`; this one is the cross-tree gate.",
  "include": [
    "packages/core/src/**/*.ts",
    "packages/client/src/**/*.ts",
    "packages/client-evm/src/**/*.ts",
    "deploy/**/*.ts",
    "circuits/**/*.ts",
    "chains/evm/test/fixtures/**/*.ts",
    "apps/indexer/src/**/*.ts",
    "apps/indexer/test/**/*.ts",
    "apps/relayer/src/**/*.ts",
    "apps/relayer/test/**/*.ts",
    "apps/sweeper/src/**/*.ts",
    "apps/sweeper/test/**/*.ts"
  ]
}
