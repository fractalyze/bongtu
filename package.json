{
  "name": "bongtu",
  "version": "0.0.0",
  "description": "bongtu monorepo root \u2014 npm workspaces (apps/*, packages/*) + the TS toolchain for the deploy/circuits/fixtures scripts, type-checked via the root tsconfig.",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@bongtu/core": "*"
  }
}
