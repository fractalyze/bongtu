# <title> — spec

Author: spec-drafter (reviewed by <name>). Status: draft.
Intent: [intent.md](intent.md)

## Requirements
Numbered R1, R2, … — each traceable to the intent's Problem or Proposed outcome, each
falsifiable. Mark MUST vs SHOULD.

## Design
How the system meets the requirements: which components change and what their new contracts
are (public surfaces, wire types, events, storage). Reference the owning docs/ files rather
than restating their facts. No file lists or work order — that is plan.md.

## Non-goals
What this deliberately does not do, so scope creep is visible in review.

## Concerns
Flagged items for the human gate — security (cite docs/security-model.md), protocol
invariants, live-pool compatibility, UX policy. Each resolved or explicitly deferred before
the spec PR merges.

## Docs debt
Which docs/ and README files must be updated when this ships.
